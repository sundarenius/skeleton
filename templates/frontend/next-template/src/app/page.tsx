import styles from './page.module.scss'
import Button from '@mui/material/Button';

const Home = () => {
  return (
    <main className={styles.main}>
      <p>NEXT TEMPLATE FROM SCRATCH</p>
      <Button variant="contained">Contained MUI button</Button>
    </main>
  )
}

export default Home;
